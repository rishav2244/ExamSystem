export const CreateExamCard = ({onClick}) => {
    return (
        <button
            className="CreateExamCard"
            onClick={onClick}>
            <h2>
                Create exam
            </h2>
        </button>
    )
}
