export const CreateUserCard = ({onClick}) => {
    return (
        <button
            className="CreateCard"
            onClick={onClick}>
            <h2>
                Create User
            </h2>
        </button>
    )
}
